# Makefile

CC = gcc
CFLAGS = -Wall -Werror

SRCS = main.c school.c
OBJS = $(SRCS:.c=.o)
EXECUTABLE = school

all: $(EXECUTABLE)

$(EXECUTABLE): $(OBJS)
	$(CC) $(CFLAGS) $(OBJS) -o $(EXECUTABLE)

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(OBJS) $(EXECUTABLE)
