#include "main.h"
/**
 * _abs - print absolute value of an integer
 * Return: positve value of an int
 * @n: is the char to be checked
 */

int _abs(int n)
{
if (n > 0)
	return (n);
else
	return (-n);
}
