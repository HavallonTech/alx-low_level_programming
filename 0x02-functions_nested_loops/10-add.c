#include "main.h"
/**
 * add - adds two integers and returns the summation
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
int result;

result = a + b;
return (result);
}
